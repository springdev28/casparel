--- a/install.js
+++ b/install.js
@@ -284,6 +284,10 @@
   if (isToPathJS) {
     validateBinaryVersion(process.execPath, toPath);
   } else {
+    try {
+      fs2.chmodSync(toPath, 493);
+    } catch {
+    }
     validateBinaryVersion(toPath);
   }
 });
